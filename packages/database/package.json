{
  "name": "@collm/database",
  "version": "0.0.1",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate --schema=./prisma/user.prisma && prisma generate --schema=./prisma/core.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}